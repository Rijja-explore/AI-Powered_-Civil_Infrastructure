# Segmentation Model Directory

This directory contains the YOLOv8 segmentation model with:
- Trained instance segmentation weights
- Mask detection metrics
- Training and validation visualizations

The model is trained for civil infrastructure crack and defect segmentation.

## Model Files
- `weights/best.pt` - Best model checkpoint
- `weights/last.pt` - Latest model checkpoint
